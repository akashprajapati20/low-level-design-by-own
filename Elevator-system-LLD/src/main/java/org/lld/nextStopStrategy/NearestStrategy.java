package org.lld.nextStopStrategy;

import org.lld.ElevatorCar;

public class NearestStrategy implements SchedulingStrategy{
    @Override
    public Integer nextStop(ElevatorCar elevatorCar) {
        return 0;
    }
}
